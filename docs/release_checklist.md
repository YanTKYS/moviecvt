# リリース前チェックリスト — 動画簡易変換ツール v0.4.2

| 項目 | 内容 |
|------|------|
| バージョン | v0.4.2（試作版・検証版） |
| 作成日 | 2026-05-23 |
| 確認者 | （確認時に記入） |

---

## 1. ビルド確認

- [ ] Windows 環境で `dotnet build -c Release` が成功する
- [ ] 警告が 0 件であること（または許容できる警告のみであること）
- [ ] セルフコンテインド発行 (`dotnet publish -r win-x64 --self-contained`) が成功する
- [ ] 発行された `MovieConverter.exe` が単体で起動する

## 1-A. 起動安定性確認（v0.3.2以降・v0.3.1での起動クラッシュ再発防止）

> v0.3.1 で `UseWpf=true` による `DllNotFoundException` 起動クラッシュが発生した教訓から追加。

- [ ] セルフコンテインド発行版（`dotnet publish -r win-x64 --self-contained`）を実機で起動する
- [ ] 起動直後にアプリが落ちない（タスクマネージャにプロセスが残り続ける）
- [ ] Windows イベントビューアー（アプリケーションログ）に `.NET Runtime` エラーが出ていない
- [ ] 起動後、プレビュー領域に「MP4ファイルを選択すると...」が表示される
- [ ] WebView2 標準プレビューでMP4を読み込みできる
- [ ] 変換処理（高速カット）が実行できる
- [ ] `logs/startup_error_*.log` が作成されていない（例外が発生していない）

## 2. 動作・環境確認

- [ ] 閉域（インターネット非接続）環境で正常に起動する
- [ ] 外部通信が発生していないことを確認した（通信ログ等で確認）
- [ ] WebView2 Runtime がインストールされた環境で動作する
- [ ] .NET 8 Desktop Runtime がインストールされた環境で動作する（またはセルフコンテインド配布）
- [ ] ffmpeg.exe 未配置時に適切なメッセージが表示される
- [ ] `bin\ffmpeg\` に ffmpeg.exe および DLL 一式（`avcodec-*.dll`、`avformat-*.dll` 等）が配置されている
- [ ] `bin\ffmpeg\ffmpeg.exe -version` を実行してバージョン情報が表示される
- [ ] ffmpeg.exe 配置後に変換が正常に動作する

---

## 2. 機能確認（基本動作）

- [ ] MP4ファイルをファイル選択で読み込める
- [ ] MP4ファイルをドラッグ＆ドロップで読み込める
- [ ] MP4以外のファイルを選択したときエラーが表示される
- [ ] 「動画全体を変換」選択時に開始・終了位置ボタンが無効になっている
- [ ] 「動画全体を変換」選択時に開始・終了位置なしで変換できる
- [ ] 「動画全体を変換」でログに「変換範囲: 動画全体」が表示される
- [ ] 「動画全体を変換」選択時に高速カット（FastCut）が自動で標準に切り替わる
- [ ] MP4をWebView2（プレビュー領域）にD&Dしてもブラウザ内再生されずアプリ内プレビューが開く
- [ ] プレビュー音量スライダーが動作する（再生中に音量が変化する）
- [ ] 消音ボタンが動作する
- [ ] 変換実行時にプログレスバーが表示される
- [ ] ステータス欄に経過時間が1秒ごとに更新表示される
- [ ] 圧縮変換で進捗率（%）がプログレスバーに反映される
- [ ] 変換中ログが簡略化されている（ffmpeg生出力が表示されない）
- [ ] 変換完了後にプログレスバーが非表示になる
- [ ] キャンセル後にプログレスバーが非表示になり再変換できる
- [ ] プレビューできないMP4を読み込むと事前変換のダイアログが表示される
- [ ] ダイアログに技術用語（faststart・moov atom等）が含まれない
- [ ] 「実行する」を押すと事前変換が開始され、進捗が表示される
- [ ] 「キャンセル」を押すと事前変換を行わない
- [ ] 事前変換完了後、変換後ファイルが自動でプレビューに読み込まれる
- [ ] 事前変換後のファイルに対してダイアログが再表示されない
- [ ] 事前変換キャンセル後に不完全ファイルが削除される
- [ ] 元ファイルが事前変換によって変更・削除されない
- [ ] ffprobe.exe 配置時にMP4読み込み後に動画情報（長さ・解像度・コーデック等）が表示される
- [ ] ffprobe.exe 未配置時に動画情報欄が空白のまま（エラー表示なし）
- [ ] 圧縮変換で進捗5%超になると残り時間（ETA）が表示される
- [ ] 進捗5%以下では「残り時間を計算中...」が表示される
- [ ] 圧縮設定の初期値が「しない（高速カット）」になっている
- [ ] 圧縮設定に「速度優先」が表示される（高速カットの次）
- [ ] 「速度優先」選択時のヒントが表示される（「変換時間を短くします。画質は少し下がる場合があります。」）
- [ ] 「速度優先」選択時に解像度コンボが有効（グレーアウトされない）
- [ ] 「速度優先」で変換実行するとログに「出力方式: 圧縮変換（速度優先）」が表示される
- [ ] 「速度優先」＋解像度変更の組み合わせで変換が正常完了する
- [ ] 「しない（高速カット）」選択時に解像度コンボが無効（グレーアウト）になっている
- [ ] 圧縮変換選択時に解像度コンボが有効になっている
- [ ] 高速カット時にログに「出力方式: 高速カット（再エンコードなし）」が表示される
- [ ] 圧縮変換時にログに「出力方式: 圧縮変換（再エンコードあり）」が表示される
- [ ] 高速カット時の ffmpeg コマンドに `-c copy` が含まれ `-c:v libx264` が含まれない
- [ ] 動画プレビューが表示される（file-uri方式で読み込まれることをログで確認）
- [ ] file-uri方式が失敗した場合に virtual-host方式へフォールバックされる（ログに「フォールバック」が表示される）
- [ ] 両方式とも失敗した場合に事前変換ダイアログが表示される
- [ ] **v0.3.2 起動確認**: セルフコンテインド発行版が起動する（DllNotFoundException が出ない）
- [ ] **v0.3.2 起動確認**: イベントビューアーにクラッシュログがない
- [ ] **v0.3.2 起動確認**: 画面右上に「プレビュー方式:」コンボボックスが表示されない
- [ ] **v0.3.2 例外ログ**: 正常起動時は `logs/startup_error_*.log` が作成されない
- [ ] **v0.3.2 例外ログ**: `logs/` フォルダが書き込み可能な場所に作成できる
- [ ] 再生・一時停止ができる
- [ ] 5秒戻る・5秒進むができる
- [ ] シークバーで任意の位置に移動できる
- [ ] 開始位置を設定できる
- [ ] 終了位置を設定できる
- [ ] 開始位置・終了位置の手入力補正ができる
- [ ] 指定範囲の秒数が表示される
- [ ] 圧縮設定（画質優先・標準・容量優先）を選択できる
- [ ] 解像度（元のまま・720p・480p）を選択できる
- [ ] 変換実行でMP4が出力される
- [ ] キャンセルで変換を中断できる
- [ ] 出力ファイル名にタイムスタンプが付く
- [ ] 変換中にUIが固まらない
- [ ] **v0.4.0 変換結果ダイアログ**: 変換完了後に結果ダイアログが表示される
- [ ] **v0.4.0 変換結果ダイアログ**: 出力ファイル名・出力先が表示される
- [ ] **v0.4.0 変換結果ダイアログ**: 変換前・変換後のファイルサイズが表示される
- [ ] **v0.4.0 変換結果ダイアログ**: 削減率（%）が表示される
- [ ] **v0.4.0 変換結果ダイアログ**: 処理時間が表示される
- [ ] **v0.4.0 変換結果ダイアログ**: 「出力フォルダを開く」ボタンでエクスプローラーが開く
- [ ] **v0.4.0 変換結果ダイアログ**: 「再生」ボタンで出力ファイルが再生される
- [ ] **v0.4.0 設定説明**: 圧縮設定を変更するとヒントテキストが変わる（全5設定）
- [ ] **v0.4.0 設定説明**: 高速カット選択時「速い。画質は変わりません。開始位置が少しずれる場合があります。」が表示される
- [ ] **v0.4.0 設定説明**: 標準選択時「迷ったときの設定です。」が表示される
- [ ] **v0.4.0 エラー診断**: ffmpeg未配置時のログメッセージが改善されている（「配置先:」「→」が表示される）
- [ ] **v0.4.0 エラー診断**: ffprobe未配置時のログに注意メッセージが表示される
- [ ] **v0.4.0 エラー診断**: DLL不足（終了コード -1073741515）時に診断メッセージが表示される
- [ ] **v0.4.0 エラー診断**: 変換失敗時のダイアログに原因の目安が表示される
- [ ] **v0.4.0 ログ保存**: 「ログを保存」ボタンが表示される
- [ ] **v0.4.0 ログ保存**: ボタン押下でテキストファイルが保存される
- [ ] **v0.4.0 ログ保存**: 保存後にフォルダを開くか確認ダイアログが表示される
- [ ] **v0.4.0 ログ保存**: ログが空の場合に「保存するログがありません」と表示される
- [ ] **v0.4.2 動作確認ボタン**: ログ欄上部に「動作確認」ボタンが表示される
- [ ] **v0.4.2 動作確認ボタン**: ボタンを押すと環境確認が実行され結果ダイアログが表示される
- [ ] **v0.4.2 動作確認**: WebView2 Runtime の有無が OK / 注意 で表示される
- [ ] **v0.4.2 動作確認**: ffmpeg.exe の有無が OK / NG で表示される
- [ ] **v0.4.2 動作確認**: ffmpeg 起動確認が OK / NG（DLL不足含む）で表示される
- [ ] **v0.4.2 動作確認**: ffprobe.exe の有無が OK / 注意 で表示される
- [ ] **v0.4.2 動作確認**: ffprobe 起動確認（配置時）が OK / NG で表示される
- [ ] **v0.4.2 動作確認**: ログフォルダへの書き込み可否が OK / 注意 で表示される
- [ ] **v0.4.2 動作確認**: ファイル選択時に動画ファイル存在・出力先書き込みが表示される
- [ ] **v0.4.2 動作確認**: 問題がある場合に案内メッセージが表示される
- [ ] **v0.4.2 診断結果保存**: 「診断結果を保存」ボタンで `logs/diagnostic_*.txt` が保存される
- [ ] **v0.4.2 起動ログ**: 起動時に `logs/startup_latest.log` が生成される
- [ ] **v0.4.2 起動ログ**: startup_latest.log にバージョン・OS・WebView2・ffmpeg情報が記録されている

---

## 3. 安全性確認

- [ ] 入力ファイルを上書き・削除しない
- [ ] 出力ファイルを上書きしない（既存ファイルが衝突した場合も安全）
- [ ] キャンセル後に不完全な出力ファイルが削除される
- [ ] 個人情報をファイルに保存しない
- [ ] ログにパスワード・個人情報が含まれない
- [ ] 外部URLへのアクセスがない
- [ ] プロセスが正常に終了する（アプリ終了時にffmpegが残らない）

---

## 4. エラー処理確認

- [ ] ffmpeg.exe 未配置時のエラーメッセージが分かりやすい
- [ ] 変換失敗時にログに詳細が表示される
- [ ] 変換失敗時にエラーダイアログの案内が分かりやすい
- [ ] 入力ファイルが存在しない場合のエラーが表示される
- [ ] 開始・終了位置が未設定の場合に変換ボタンが無効

---

## 5. ドキュメント確認

- [ ] README.md が整備されている
- [ ] docs/tool_design.md が整備されている
- [ ] docs/release_checklist.md（本文書）が整備されている
- [ ] docs/test_scenarios.md が整備されている
- [ ] docs/operation_handover.md が整備されている
- [ ] docs/ffmpeg_setup.md が整備されている
- [ ] docs/license_notice.md が整備されている
- [ ] manuals/admin_manual.md が整備されている
- [ ] manuals/operator_manual.md が整備されている
- [ ] manuals/user_manual.md が整備されている
- [ ] development_report.md が整備されている

---

## 6. バージョン区分の確認

| 区分 | 内容 |
|------|------|
| v0.1.0 | 試作版・検証版（本バージョン） |
| v0.x.x | 機能追加・改修中の検証版 |
| v0.9.x | 本番移行前の最終検証版 |
| v1.0.0 | 本番版（正式リリース） |

- [ ] 本バージョンが試作版（v0.x.x）であることがREADMEに明記されている
- [ ] 試作版であることを利用者・管理者が認識している

---

## 7. 初期版の対象外事項の確認

以下が対象外であることを関係者が理解しているか確認する。

- [ ] MP4以外の動画形式（AVI, MOV, MKS等）は対象外
- [ ] 複数ファイルの一括変換は対象外
- [ ] 複数区間の切り出しは対象外
- [ ] 動画結合は対象外
- [ ] 字幕・音声トラック編集は対象外
- [ ] フレーム単位の厳密編集は対象外

---

## 8. 配布物の確認

- [ ] `MovieConverter.exe`（またはセルフコンテインド配布物）が含まれる
- [ ] `bin/ffmpeg/ffmpeg.exe`（またはその配置手順）が提供されている
- [ ] `docs/ffmpeg_setup.md` が含まれる
- [ ] `docs/license_notice.md` が含まれる
- [ ] `manuals/user_manual.md` が含まれる
- [ ] 不要な開発ファイル（.git, bin/Debug等）が含まれていない

---

---

## v0.1.1 完了条件チェック

- [ ] ビルドできる（Windows 環境）
- [ ] アプリが起動する
- [ ] MP4を読み込める（日本語・空白パス含む）
- [ ] プレビュー再生できる
- [ ] 開始・終了位置を指定できる
- [ ] 変換できる（全プリセット）
- [ ] キャンセルできる
- [ ] ffmpeg未配置時の案内が分かりやすい
- [ ] development_report.mdに確認結果が記録されている

---

*作成: 2026-05-23  更新: 2026-05-24  バージョン: v0.4.0*
