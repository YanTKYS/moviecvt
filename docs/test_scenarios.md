# テストシナリオ — 動画簡易変換ツール v0.1.3

| 項目 | 内容 |
|------|------|
| 対象バージョン | v0.1.3 |
| テスト実施日 | （実施時に記入） |
| 実施者 | （実施時に記入） |
| 環境 | Windows 10/11、ffmpeg.exe 配置済み |

---

## 1. 前提条件

- ffmpeg.exe が `bin\ffmpeg\ffmpeg.exe` に配置されていること
- WebView2 Runtime（Microsoft Edge WebView2 Runtime）がインストールされていること
- テスト用 MP4 ファイルが用意されていること（3〜5分程度を推奨）
- テスト用非MP4ファイルが用意されていること（例: test.avi, test.txt）

---

## 2. 正常系テスト

### 2-1. MP4ファイルの選択（ファイルダイアログ）

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 「ファイルを選択...」ボタンを押す | ファイル選択ダイアログが開く | - | |
| MP4ファイルを選択してOKを押す | ファイルパスとサイズが表示される | - | |
| 動画プレビューが表示される | プレビュー欄に動画が表示される | - | |
| 再生ボタンが有効になる | 再生・開始設定・終了設定ボタンが押せる | - | |

### 2-2. MP4ファイルのドラッグ＆ドロップ

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| ウィンドウにMP4ファイルをドラッグする | カーソルが変化する（コピー可表示） | - | |
| ドロップする | ファイルが読み込まれ、プレビューが表示される | - | |

### 2-3. 動画の再生・一時停止

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 「▶ 再生」ボタンを押す | 動画が再生され、ボタンが「⏸ 一時停止」に変わる | - | |
| 「⏸ 一時停止」ボタンを押す | 動画が一時停止し、ボタンが「▶ 再生」に戻る | - | |
| 現在時刻表示が更新される | シークバーと時刻表示が再生に追随する | - | |

### 2-4. 5秒戻る・5秒進む

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 動画の途中で「◀ 5秒戻る」を押す | 5秒前に移動する | - | |
| 「5秒進む ▶」を押す | 5秒後に移動する | - | |
| 動画先頭付近で「◀ 5秒戻る」を押す | 0秒以下にはならない | - | |
| 動画末尾付近で「5秒進む ▶」を押す | 動画の長さを超えない | - | |

### 2-5. シークバーによる移動

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| シークバーをドラッグする | 動画がその位置に移動する | - | |
| 時刻表示が更新される | 現在時刻表示がシークバーと一致する | - | |

### 2-6. 開始位置・終了位置の設定

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 任意の位置で「現在位置を開始に設定」を押す | 開始位置テキストボックスに時刻が入る | - | |
| 別の位置で「現在位置を終了に設定」を押す | 終了位置テキストボックスに時刻が入る | - | |
| 「指定範囲」が表示される | 開始〜終了の秒数が表示される | - | |
| 「変換実行」ボタンが有効になる | ボタンが押せる状態になる | - | |

### 2-7. 開始・終了位置の手入力補正

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 開始時刻テキストボックスに `00:01:30` と入力してTabを押す | 時刻が 00:01:30 に更新される | - | |
| 終了時刻テキストボックスに `00:03:00` と入力してTabを押す | 時刻が 00:03:00 に更新される | - | |

### 2-8. 標準・720pで変換

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 圧縮設定「標準」、解像度「720p」を選択 | 既定値のため選択済み | - | |
| 開始・終了位置を設定して「変換実行」を押す | ログ欄に処理が表示される | - | |
| 完了メッセージが表示される | 出力ファイルパスとサイズが表示される | - | |

### 2-9. 出力ファイルの確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 出力ファイルが作成されている | `{元ファイル名}_cut_{タイムスタンプ}.mp4` が存在する | - | |
| 出力ファイルが再生できる | 標準の動画プレイヤーで再生できる | - | |
| 出力ファイルの長さが正しい | 指定した範囲に近い長さになっている | - | |
| 解像度が720p以下である | 動画プロパティで確認できる | - | |

### 2-10. 画質優先・容量優先でも変換できる

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 圧縮設定「画質優先」で変換 | 変換成功、「標準」より大きいファイルが生成される | - | |
| 圧縮設定「容量優先」で変換 | 変換成功、「標準」より小さいファイルが生成される | - | |

### 2-11. 解像度「元のまま」「480p」でも変換できる

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 解像度「元のまま」で変換 | 入力と同じ解像度で出力される | - | |
| 解像度「480p」で変換 | 高さ480pxの動画が出力される | - | |

---

## 3. 異常系テスト

### 3-1. MP4以外を選択した場合

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| ファイル選択で .avi ファイルを選ぶ | 「対応していないファイル形式です」のエラーが表示される | - | |
| ドラッグ＆ドロップで .txt をドロップ | 同様のエラーが表示される | - | |
| 入力ファイルはクリアされない | 以前の動画設定が維持される | - | |

### 3-2. ffmpeg.exeが存在しない場合

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| ffmpeg.exe を配置せずに起動 | 起動時にログに警告が表示される | - | |
| ステータスバーに警告が表示される | 「⚠ ffmpeg.exe 未配置」と表示される | - | |
| 変換実行ボタンが無効 | ボタンが押せない状態 | - | |
| 変換実行を試みる（バリデーション確認） | エラーダイアログが表示される | - | |

### 3-3. 開始位置が終了位置以降の場合

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 開始位置 > 終了位置 になるよう設定 | 指定範囲欄に「終了位置が開始以前です」と表示される | - | |
| 「変換実行」ボタンが無効 | ボタンが押せない | - | |

### 3-4. 入力ファイルが存在しない場合（選択後に削除された場合）

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| ファイルを選択後、そのファイルを削除する | （この状態で）変換実行を押す | - | |
| エラーダイアログが表示される | 「ファイルが見つかりません」と表示される | - | |

### 3-5. 出力先に書き込みできない場合

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 読み取り専用フォルダのMP4を選択して変換 | 変換失敗のエラーが表示される | - | |
| ログに詳細が表示される | FFmpegのエラーメッセージがログ欄に表示される | - | |

### 3-6. 変換中にキャンセルする

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 変換実行を押す | 「キャンセル」ボタンが有効になる | - | |
| 「キャンセル」ボタンを押す | 変換処理が中断される | - | |
| ステータスが「キャンセルされました」になる | - | - | |
| 不完全な出力ファイルが削除される | 中途半端なファイルが残らない | - | |
| 再び変換できる | 「変換実行」ボタンが有効に戻る | - | |

---

## 4. 再実行系テスト

### 4-1. 同じ動画を再度変換しても既存ファイルを上書きしない

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 同じ設定で複数回変換する | タイムスタンプが異なる別ファイルが作成される | - | |
| 既存ファイルは保持される | 上書きされていない | - | |

### 4-2. 変換失敗後に条件を修正して再実行できる

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 変換に失敗させる（書き込み不可の場所等） | エラー表示後、「変換実行」が再び有効 | - | |
| 設定を修正して再度変換実行 | 変換が成功する | - | |

### 4-3. キャンセル後に再実行できる

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| キャンセルする | ステータスが「キャンセルされました」 | - | |
| 再度「変換実行」を押す | 問題なく変換が開始される | - | |

---

## 5. UI/UX テスト

| 確認項目 | 期待結果 | 結果 | 備考 |
|----------|----------|------|------|
| 変換中にUIが固まらない | 変換中もウィンドウを移動・操作できる | - | |
| ログが自動スクロールする | 新しいログが追加されたとき末尾が表示される | - | |
| 専門用語が少ない | 一般職員が見て分かる表現 | - | |
| エラーメッセージに対処が書かれている | 「〜してください」という案内がある | - | |

---

## 6. ファイル・パス関連テスト

| 確認項目 | 期待結果 | 結果 | 備考 |
|----------|----------|------|------|
| スペースを含むパスで動作する | 例: `C:\My Videos\test.mp4` | - | |
| 日本語フォルダ名で動作する | 例: `D:\動画素材\会議録.mp4` | - | |
| 長いパスで動作する | 200文字程度のパス | - | |

---

## 7. v0.1.1 追加確認項目

### 7-1. WebView2 初期化タイミング（v0.1.1 fix）

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| アプリ起動直後（WebView2 初期化中）にMP4をドラッグ＆ドロップ | WebView2 初期化完了後にプレビューに反映される | - | v0.1.1で修正 |

### 7-2. 動画エラー時の挙動（v0.1.1 fix）

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 破損したMP4や非対応コーデックのファイルを開く | プレビュー内にエラーメッセージが表示される | - | v0.1.1で改善 |
| ステータスバーが「動画の読み込みに失敗しました」になる | - | - | |
| 再生・変換ボタンが無効になる | - | - | |

### 7-3. 日本語・空白パスのテスト

| 確認項目 | 期待結果 | 結果 | 備考 |
|----------|----------|------|------|
| 日本語フォルダ: `D:\動画素材\` | 正常に読み込める | - | |
| 日本語ファイル名: `会議録画.mp4` | 正常に読み込める | - | |
| スペース入りフォルダ: `C:\My Videos\` | 正常に読み込める | - | |
| スペース入りファイル名: `会議 録画.mp4` | 正常に読み込める | - | |
| 括弧入りファイル名: `meeting(2026).mp4` | 正常に読み込める | - | |

### 7-4. セルフコンテインド発行後の確認

| 確認項目 | 期待結果 | 結果 | 備考 |
|----------|----------|------|------|
| `dotnet publish -r win-x64 --self-contained true -p:PublishSingleFile=true` | エラーなく発行できる | - | |
| 発行後の `MovieConverter.exe` 単体で起動する | .NET ランタイムなしで起動 | - | |
| `bin\ffmpeg\ffmpeg.exe` の検索パスが正しい | 実行ファイルと同じフォルダの `bin\ffmpeg\` を参照 | - | v0.1.1で修正 |

## 8. v0.1.2 追加確認項目

### 8-1. プレビュー読み込み方式の確認（v0.1.2 新規）

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 通常のMP4を読み込む | ログに「file-uri方式で読み込みます」「読み込み成功 方式: file-uri」が表示される | - | v0.1.2で主方式変更 |
| 読み込み成功後にログを確認する | 「file-uri」で成功していることがログに記録されている | - | |

### 8-2. Edgeで再生できるが本ツールで再生できないMP4の切り分け確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 対象MP4をMicrosoft Edgeで直接ドラッグ＆ドロップする | Edgeで再生できることを確認 | - | |
| 同じMP4を本ツールで読み込む | ログに「file-uri方式」の試行が記録される | - | |
| file-uri方式が失敗した場合 | ログに「file-uri方式が失敗しました。virtual-host方式を試みます。」が表示される | - | |
| virtual-host方式でリトライされる | ログに「virtual-host方式で読み込みます」が表示される | - | |
| virtual-host方式でも失敗した場合 | プレイヤーにエラーが表示され、ログに確認方法（3ステップ）が出力される | - | |
| エラーメッセージが誤解を招かない表現になっている | 「Microsoft Edgeで再生できる動画でも〜」というメッセージが表示される | - | v0.1.2で改善 |
| プレビュー不可の場合、変換ボタンが無効になっている | 開始・終了位置を設定できないため変換実行ができない | - | 初期版の仕様 |

### 8-3. MP4/AVC プレビュー失敗時の切り分け手順

プレビューに失敗した場合、以下の手順で原因を切り分けてください。

#### ステップ 1: Microsoft Edge での直接再生確認

対象のMP4ファイルをMicrosoft Edgeで直接開いて再生できるか確認する。

- Edge で再生できない場合 → 動画ファイル自体に問題がある可能性が高い
- Edge で再生できる場合 → 本ツール（WebView2）の読み込み方式との相性問題の可能性あり

#### ステップ 2: 短いサンプルで確認

長時間・大容量動画の場合、30秒程度のサンプルを切り出して確認する。

```bash
ffmpeg -ss 00:10:00 -t 00:00:30 -i input.mp4 -c copy sample_30sec.mp4
```

- サンプルでは再生できる場合 → 動画の長さ・容量がWebView2プレビューの制約に引っかかっている可能性あり

#### ステップ 3: faststart 化で確認

MP4のメタデータ位置（moov atom）が末尾にある場合、先頭への移動（faststart）で改善する場合がある。

```bash
ffmpeg -i input.mp4 -c copy -movflags +faststart output_faststart.mp4
```

- faststart化後に再生できる場合 → moov atom 末尾配置がWebView2での読み込み失敗の原因だった
- faststart化後も再生できない場合 → 他の要因を確認する

#### 切り分け結果の記録

| 確認項目 | 結果 | 備考 |
|----------|------|------|
| Edge で直接再生できるか | - | |
| 30秒サンプルで再生できるか | - | |
| faststart化後に再生できるか | - | |

---

## 9. プレビュー不可時の変換仕様確認

- [ ] プレビューができない場合、開始・終了位置を設定できない（ボタン無効）
- [ ] 開始・終了位置が未設定のため、変換実行ボタンが無効のままになる
- [ ] ドキュメント（README・user_manual）に「プレビューできない場合は変換できない」旨が記載されている

---

## 10. v0.1.3 追加確認項目

### 10-0. v0.1.4 追加確認項目

#### 全体変換モード

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 「動画全体を変換」を選択する | 開始・終了位置ボタンがグレーアウトされる | - | v0.1.4 |
| 開始・終了位置を設定せずに「変換実行」を押す | 変換が正常実行される | - | 全体変換時は設定不要 |
| ログに「変換範囲: 動画全体」が表示される | - | - | |
| 「動画全体を変換」選択時、圧縮設定が「しない（高速カット）」になっていた場合 | 自動的に「標準」に切り替わる | - | |
| 「選択範囲を変換」に戻す | 開始・終了位置ボタンが再び有効になる | - | |

#### D&D挙動修正

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| MP4ファイルをプレビュー領域（WebView2）にドラッグ＆ドロップする | WebView2内でブラウザ再生されず、アプリ内プレビューに読み込まれる | - | v0.1.4修正 |
| アプリ画面外縁部にD&Dしたときも同様に動作する | ファイルパスとサイズが表示され、プレビューが読み込まれる | - | |

#### プレビュー音量

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 動画読み込み後に音量スライダーを操作する | プレビューの音量が変化する | - | v0.1.4 |
| 「消音」ボタンを押す | プレビューがミュートになる | - | |
| 「音有」ボタン（消音後の表示）を押す | ミュートが解除される | - | |
| 音量スライダーを動かす | ミュートが解除される | - | スライダー操作でミュート自動解除 |
| 出力ファイルの音量が変化していないこと | 変換後のMP4を再生して確認 | - | プレビュー音量のみ変更 |

---

### 10-1. 高速カット動作確認（v0.1.3 新規）

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| アプリ起動直後の圧縮設定コンボを確認する | 「しない（高速カット）」が選択されている | - | 初期値の確認 |
| 「しない（高速カット）」を選択した状態で解像度コンボを確認する | 解像度コンボがグレーアウトされて操作できない | - | v0.1.3で無効化 |
| 「画質優先」等の圧縮変換を選択する | 解像度コンボが有効になる | - | |
| 「しない（高速カット）」に戻す | 解像度コンボが再び無効になる | - | |
| 画面のヒントテキストを確認する | 「高速カットは再圧縮しないため高速ですが、開始位置が少しずれる場合があります。」が表示される | - | |

### 10-2. 高速カット変換実行確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 「しない（高速カット）」で変換実行する | ログに「出力方式: 高速カット（再エンコードなし）」が表示される | - | v0.1.3 |
| ffmpeg実行コマンドを確認する | ログに `-c copy` が含まれ、`-c:v libx264` が含まれない | - | |
| 出力ファイルが作成されている | 変換成功・ファイルが再生できる | - | |
| 長時間動画（1時間以上）で高速カットを実行する | 短時間（数秒〜数十秒）で完了する | - | 再エンコードなしのため高速 |

### 10-3. 圧縮変換のログ確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 「標準」を選択して変換実行する | ログに「出力方式: 圧縮変換（再エンコードあり）」が表示される | - | |
| ログに品質・解像度が表示される | 「品質: 標準 / 解像度: 720p」等が表示される | - | |

### 10-4. ffmpeg DLL 配置確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| `bin\ffmpeg\ffmpeg.exe` および DLL 一式を配置する | フォルダ内に `avcodec-*.dll`、`avformat-*.dll` 等が存在する | - | v0.1.3で手順明記 |
| コマンドプロンプトで `bin\ffmpeg\ffmpeg.exe -version` を実行する | バージョン情報が表示される（エラーなし） | - | DLL不足の場合は表示されない |
| バージョン情報表示後にアプリを起動して変換を実行する | 変換が正常完了する | - | |

### 10-5. DLL 不足時の終了コード確認（-1073741515）

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| `bin\ffmpeg\` に `ffmpeg.exe` のみ配置してDLLを配置しない | - | - | DLL欠落状態を再現 |
| 変換実行を行う | ログに「終了コード: -1073741515」が表示される | - | DLL不足を示す終了コード |
| エラーメッセージに対処内容が含まれている | 「DLL一式を配置してください」等の案内が表示される | - | |
| DLLを追加配置して再実行する | 変換が正常完了する | - | |

---

## 11. v0.1.5 追加確認項目（進捗表示）

### 11-1. プログレスバーの表示確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 圧縮変換で変換実行する | 変換パネルにプログレスバーが表示される | - | v0.1.5 |
| 変換開始直後はマーキー表示になる | アニメーションするバーが表示される | - | 進捗取得前 |
| ffmpegが進捗を出力し始めると確定表示に切り替わる | 0〜100%の確定バーに変化する | - | `time=` パース成功時 |
| 高速カット変換では短時間で完了する | プログレスバーが短時間で消える | - | -c copyは高速 |
| 変換完了後にプログレスバーが非表示になる | バーが消えて通常のUIに戻る | - | |

### 11-2. 経過時間表示の確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 変換実行後のステータス欄を確認する | 「状態: 変換中... 経過時間 00:00:00」が表示される | - | v0.1.5 |
| 1秒後にステータスを確認する | 経過時間が「00:00:01」に更新される | - | |
| 進捗率が取得できる場合 | 「経過時間 00:00:05  30%」のように進捗率も表示される | - | |
| 変換完了ログを確認する | 「所要時間: 00:05:12」のような所要時間が記録されている | - | |

### 11-3. ログ簡略化の確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 変換実行後のログ欄を確認する | 「変換中です。しばらくお待ちください。」が表示される | - | v0.1.5 |
| 圧縮変換の場合のログ | 「長時間動画や圧縮変換では時間がかかります。」も表示される | - | |
| ffmpegの生の進捗行がログに表示されない | `frame=...fps=...` のような行はログに出ない | - | ユーザー向け簡略化 |
| ffmpegの起動ログ・ストリーム情報がログに表示されない | `ffmpeg version...` 等の行はログに出ない | - | |
| 変換失敗時にffmpeg生ログがダンプされる | `[詳細ログ]` セクションにffmpegの出力が表示される | - | 管理者確認用 |

### 11-4. キャンセル後の状態確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 変換中に「キャンセル」ボタンを押す | プログレスバーが非表示になる | - | v0.1.5 |
| ステータスが「キャンセルされました」になる | 経過時間タイマーが停止する | - | |
| 再度「変換実行」を押す | プログレスバーが再表示される | - | |

---

## 12. v0.2.0 追加確認項目（MP4整備ボタン）

### 12-1. ボタンの有効/無効状態の確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| アプリ起動直後に「MP4を整える」ボタンを確認する | ボタンが無効（グレー）になっている | - | 動画未読み込みのため |
| MP4を読み込む | ボタンが有効になる | - | v0.2.0 |
| 変換実行中は「MP4を整える」ボタンが無効になる | 操作できない | - | 排他制御 |
| MP4整備中は「変換実行」ボタンが無効になる | 操作できない | - | 排他制御 |

### 12-2. MP4整備の正常動作確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| MP4を読み込み「MP4を整える」ボタンを押す | ログに「MP4整備開始」が表示される | - | v0.2.0 |
| 処理中のログを確認する | 「MP4を整えています。大容量ファイルでは時間がかかる場合があります。」が表示される | - | |
| 経過時間タイマーが動作する | ステータスに「状態: MP4整備中... 経過時間 00:00:xx」が表示される | - | |
| 完了時のログを確認する | 「MP4構造の整備が完了しました。作成したファイルをプレビューに読み込みます。」が表示される | - | |
| 完了後にプレビューが切り替わる | 整備後のファイルがプレビューに自動読み込みされる | - | |
| 出力ファイル名を確認する | `{元ファイル名}_faststart_{タイムスタンプ}.mp4` が作成されている | - | |
| 元ファイルが変更されていない | 元のMP4ファイルがそのまま残っている | - | |

### 12-3. MP4整備のキャンセル確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 「MP4を整える」を押して処理中に「キャンセル」を押す | 処理が中断される | - | |
| キャンセル後のログを確認する | 「MP4整備をキャンセルしました。」が表示される | - | |
| 不完全な出力ファイルが削除されている | 中途半端なファイルが残らない | - | |
| キャンセル後に「MP4を整える」が再び有効になる | ボタンが押せる状態に戻る | - | |

### 12-4. 整備後のプレビューと変換の確認

| 手順 | 期待結果 | 結果 | 備考 |
|------|----------|------|------|
| 整備後のファイルがプレビューに読み込まれた後、変換実行する | 整備後のファイルを対象に変換できる | - | |
| 整備後ファイルの動画時間が元と同じである | 全体変換した場合、時間が変わらない | - | `-c copy` のため |

---

## 13. 未実施・未確認項目

| 項目 | 理由 |
|------|------|
| Windows 実機での総合動作確認 | Windows 環境が必要 |
| 高解像度（4K）動画でのテスト | テストファイルが必要 |
| 長時間動画（1時間超）でのテスト | 確認が必要 |
| セルフコンテインド発行後の実機確認 | Windows 環境が必要 |
| DLL不足（終了コード -1073741515）の実機確認 | Windows 環境が必要 |

---

*作成: 2026-05-23  更新: 2026-05-24  バージョン: v0.2.0*
