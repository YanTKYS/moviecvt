# 動画簡易変換ツール

> **バージョン: v0.1.2（試作版・検証版）**
>
> 本バージョンは試作版です。本番利用の前に動作確認・ライセンス確認を行ってください。

---

## 概要

一般職員が専門的な動画編集ソフトを使わずに、MP4動画の切り出しとサイズ縮小を行うためのWindowsデスクトップアプリです。

庁内共有・メール添付・資料添付に適した動画ファイルを、職員自身が簡単に作成できるようにすることを目的としています。

---

## 対象利用者

庁内の一般職員（動画編集の専門知識は不要）

---

## 利用環境

| 項目 | 内容 |
|------|------|
| OS | Windows 10 (20H2以降推奨) / Windows 11 |
| ネットワーク | 閉域・庁内ネットワーク（インターネット接続不要） |
| 管理者権限 | 不要（一般ユーザー権限で実行可能） |
| .NET | .NET 8 Desktop Runtime（セルフコンテインド配布の場合は不要） |
| WebView2 | Microsoft Edge WebView2 Runtime（Windows 10 20H2以降・Windows 11は標準済み） |

---

## できること

- MP4ファイルを選択・プレビューする
- 動画を再生して開始位置・終了位置を指定する
- 指定した範囲を切り出す
- 圧縮設定（画質優先・標準・容量優先）と解像度（元のまま・720p・480p）を選択する
- 切り出した動画をMP4として出力する

## できないこと（対象外）

- MP4以外の動画形式（AVI, MOV, MKS等）の変換
- 複数ファイルの一括変換
- 複数区間の切り出し
- 動画の結合
- 字幕・音声トラックの編集
- フレーム単位の厳密な編集
- 外部サーバへのアップロード

---

## セットアップ

### 1. アプリの配置

配布パッケージを任意のフォルダに展開します。

```
MovieConverter/
├── MovieConverter.exe
├── Assets/
│   └── player.html
├── bin/
│   └── ffmpeg/
│       └── ffmpeg.exe    ← 別途配置が必要（下記参照）
├── docs/
└── manuals/
```

### 2. ffmpeg.exe の配置

動画変換には `ffmpeg.exe` が必要です。
`docs/ffmpeg_setup.md` を参照して、`bin\ffmpeg\ffmpeg.exe` に配置してください。

### 3. 起動

`MovieConverter.exe` をダブルクリックで起動します。

---

## 使い方（概要）

1. **「ファイルを選択...」** でMP4ファイルを選ぶ（またはドラッグ＆ドロップ）
2. プレビューで動画を確認し、**「現在位置を開始に設定」「現在位置を終了に設定」** で範囲を指定する
3. 圧縮設定・解像度を選択する（既定値: 標準・720p）
4. **「変換実行」** を押して変換する

詳細は `manuals/user_manual.md` を参照してください。

---

## 出力ファイル

- 保存先: 入力ファイルと同じフォルダ
- ファイル名: `{元のファイル名}_cut_{yyyyMMdd_HHmmss}.mp4`
- 例: `meeting_cut_20260523_153000.mp4`

入力ファイルは上書きされません。

---

## 個人情報の取扱い

- アプリは個人情報をファイルに保存しません
- 外部サーバへの送信を行いません
- 動画ファイルの内容・管理は利用者・利用部署の責任で行ってください

---

## 対応形式と注意事項

### 対応形式

初期版は **MP4 / AVC(H.264) / AAC** を主対象としています。

### プレビューに関する注意

- 動画プレビューは `file://` URI 方式でロードします（v0.1.2で仮想ホスト方式から変更）
- `file://` 方式が失敗した場合は、仮想ホスト（`https://video.local/`）方式に自動フォールバックします
- MP4ファイルであっても、両方式ともプレビューできない場合があります
- プレビューできない場合は、**Microsoft Edge** でそのファイルを直接開いて再生できるか確認してください
- **初期版では、プレビューできないMP4は変換できません**（カット位置を指定するためプレビューが必要）
- 解決しない場合は管理者またはDX担当に相談してください

### その他の注意

- ffmpeg.exe のライセンスについては `docs/license_notice.md` を確認してください
- 変換処理中はPCへの負荷がかかります。長時間動画の変換は時間がかかる場合があります
- WebView2 Runtime が必要です（Windows 10 20H2以降・Windows 11は標準でインストール済み）

---

## ドキュメント一覧

| ファイル | 内容 |
|----------|------|
| `manuals/user_manual.md` | 利用者向け操作手順 |
| `manuals/operator_manual.md` | 運用担当部署向け一次対応手順 |
| `manuals/admin_manual.md` | 管理者向け配置・更新・障害対応手順 |
| `docs/tool_design.md` | 技術設計書（実装方針・判断理由） |
| `docs/ffmpeg_setup.md` | ffmpeg.exe の配置手順 |
| `docs/license_notice.md` | ライセンス確認事項 |
| `docs/test_scenarios.md` | テストシナリオ |
| `docs/release_checklist.md` | リリース前チェックリスト |
| `docs/operation_handover.md` | 運用引継ぎ文書 |
| `development_report.md` | 開発報告書（判断・未対応事項） |

---

## バージョン履歴

| バージョン | 日付 | 内容 |
|-----------|------|------|
| v0.1.2 | 2026-05-23 | MP4/AVCプレビュー失敗時のエラーメッセージ改善・切り分け手順整備 |
| v0.1.1 | 2026-05-23 | コードレビュー修正・ドキュメント整備（実機確認準備版） |
| v0.1.0 | 2026-05-23 | 初期試作版：MP4の切り出し・サイズ縮小機能 |

---

## 動作確認方法

`docs/test_scenarios.md` のテストシナリオに従って確認してください。

---

---

## ビルド方法

```bash
cd src/MovieConverter
dotnet restore
dotnet build -c Release
```

**セルフコンテインド単一ファイル発行（推奨）:**

```bash
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o publish/
```

セルフコンテインド発行では、`publish/` に `MovieConverter.exe` が生成されます。
この場合、.NET 8 Desktop Runtime のインストールが不要になります。

> **注意:** ビルドには Windows 環境（またはWindowsDesktop SDK）が必要です。
> Linux 環境では `net8.0-windows` (WinForms) のビルドはできません。

---

*DX推進担当 — 2026-05-23*
